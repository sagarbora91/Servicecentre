include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "lib/app/l10n/app_localizations*.dart" # generated by flutter gen-l10n
    - "lib/firebase_options.dart" # generated placeholder (flutterfire configure)
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    public_member_api_docs: false
    # The codebase consistently uses relative imports within lib/; don't also
    # demand package: self-imports (the two lints are mutually exclusive).
    always_use_package_imports: false
    # Hand-written value types historically defined ==/hashCode; now superseded
    # by freezed, but keep this off so any remaining hand-written value type is
    # not forced to add @immutable.
    avoid_equals_and_hash_code_on_mutable_classes: false
    # 80 columns is overly restrictive for doc comments and long string literals;
    # dart format still targets 80 for wrappable code.
    lines_longer_than_80_chars: false
